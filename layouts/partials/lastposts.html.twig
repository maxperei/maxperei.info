      <div class="lastposts">
        <h2>Récemment</h2>
        <ul>
{% for post in site.pages|filterBy('section', 'blog')|sortByDate|slice(1, 5) %}
          <li><a href="{{ url(post) }}">{{ post.title }}</a></li>
{% endfor %}
        </ul>
      </div>
